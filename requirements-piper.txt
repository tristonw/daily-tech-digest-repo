# 自托管 TTS（provider=piper）所需依赖——开源、输出音频可商用，无对外发布授权风险。
# 安装前建议先装 CPU 版 PyTorch（体积更小，且满足 piper-tts[zh] 的 torch 依赖）：
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
# 然后：
#   pip install -r requirements-piper.txt
#
# piper-tts[zh] 会带齐中文链路依赖：g2pW、sentence-stream、unicode-rbnf、requests、torch。
piper-tts[zh]>=1.2.0
lameenc>=1.7.0
requests>=2.28.0
